use nalgebra::{RealField, Scalar};
use num::Float;

use crate::particle::Particle;

pub trait Interpolator<T, const D: usize>
where
    T: Scalar + RealField + Float,
{
    fn apply(particles: Vec<Particle<T, D>>);
}
